export interface Status {
    Id: number;
    name: string;
    code: string; 
    dateCreated: Date;
}

export interface StatusResponse {
    id: number;
    name: string;
    code: string; 
}
  